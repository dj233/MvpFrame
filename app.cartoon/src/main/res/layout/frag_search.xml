<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent" android:background="@color/colorPrimary"
        android:layout_height="?attr/actionBarSize" android:padding="5dp">
        <TextView android:id="@+id/tv_search" android:paddingLeft="5dp" android:textColor="@color/white" android:textSize="20sp"
            style="@style/item_h1" android:text="@string/menu_search" android:layout_centerVertical="true" />
        <EditText
            android:id="@+id/et_search"
            android:layout_toLeftOf="@+id/iv_search"
            android:layout_toRightOf="@+id/tv_search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="5dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:background="@drawable/bg_et_5"
            android:layout_centerVertical="true"
            />
        <ImageView
            android:id="@+id/iv_search"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="5dp" android:layout_marginRight="5dp"
            android:src="@drawable/ic_icon_search"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            />
    </RelativeLayout>
</LinearLayout>